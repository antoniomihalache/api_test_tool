FROM grafana/k6:0.54.0

# Nothing extra needed – k6 binary is the entry point.
# This Dockerfile exists to allow pinning and future extensions
# (e.g. xk6 extensions).

LABEL org.opencontainers.image.description="k6 load testing runner for perf-platform"
